import type { CSSProperties } from 'react';

export default function BuildingMarquee() {
  return (
    <div
      data-framer-name={"Building"}
      className={"framer-u0kwg8"}
      dangerouslySetInnerHTML={{ __html: "" }}
    />
  );
}
